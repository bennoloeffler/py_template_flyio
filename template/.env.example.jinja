# Environment Configuration for {{ project_name }}
# Copy this file to .env and customize values for your environment

# Application Settings
ENVIRONMENT=development
DEBUG=true
APP_TITLE={{ project_name }}
APP_VERSION=0.1.0

# Server Settings
HOST=0.0.0.0
PORT=8000
WORKERS=1

# CORS Settings (comma-separated)
ALLOWED_ORIGINS=http://localhost:8000,http://localhost:3000,http://localhost:5173

# Database Settings
DATABASE_URL=postgresql://postgres:postgres@localhost/{{ module_name }}
DATABASE_TEST_URL=postgresql://postgres:postgres@localhost/{{ module_name }}_test

# Security Settings
SECRET_KEY=change-me-in-production

# Admin Credentials (change in production!)
ADMIN_PASSWORD=admin123

# Rate Limiting
DEFAULT_RATE_LIMIT=100/minute
HEALTH_RATE_LIMIT=10/minute

# External Services
OPENAI_API_KEY=your-openai-key-here

# Fly.io Specific (for production deployment)
# These are set via flyctl secrets set, not .env
# FLY_APP_NAME={{ project_name }}
# FLY_REGION=fra